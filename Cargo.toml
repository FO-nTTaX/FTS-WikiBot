[package]
name = "ftswikibot"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.12", features = [ "blocking", "cookies", "gzip", "json" ] }
serde_json = { version = "1.0", features = [ "std" ] }
